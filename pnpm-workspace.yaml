# JavaScript packages share one lockfile; mise owns the toolchain versions.
packages:
  - packages/*

allowBuilds: {}
pmOnFail: error

# Use the same script and argument semantics on Windows and POSIX hosts.
shellEmulator: true
